笔记文件，任何知识点，如果不方便写在cpp文件中，可以写在这里

1.exit()和return的区别
    main函数中无区别，在其它函数中，return结束当前函数，exit结束当前进程
    参数0表示正常退出，-1表示异常退出

2.void EXIT(int sig)
    程序的退出函数，不直接使用exit退出，而是封装成一个函数，目的如下：
    1.日志记录程序退出
    2.释放资源（大部分资源的释放都是通过析构函数执行，这里一般不用考虑）
    3.通知子线程退出
    4.作为退出信号（2，15）的处理函数